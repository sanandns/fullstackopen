```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes note in SPA and clicks Save

    browser->>server: POST /new_note_spa
    activate server
    server-->>browser: JSON response (new note)
    deactivate server

    Note right of browser: Browser updates notes list using JavaScript without reloading the page
```
